import { PageProps } from "@/types";

export default function ListAndEditTeamsForm ({ auth, message, userId }: PageProps) {
    
    return (
        <>
        <h1>ユーザー所属チーム一覧・編集</h1>
        
        </>
    );
}